name: mobx_dark_theme
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^8.8.1

  mobx: ^1.1.1
  cupertino_icons: ^0.1.3
  shared_preferences: ^0.5.7+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mobx_codegen: ^1.0.3
  build_runner: ^1.9.0

flutter:
  uses-material-design: true
